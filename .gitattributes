# Preserve content-addressed dependency snapshots byte-for-byte on every platform.
# Vendored source retains upstream whitespace and is excluded from first-party
# whitespace diagnostics; dependency identities are verified separately.
testing/v4/dependencies/** -text -whitespace
testing/v4/reports/*.gz binary
